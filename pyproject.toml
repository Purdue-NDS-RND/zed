[project]
name = "zed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyzed",
]

[tool.uv.sources]
pyzed = { path = "../../../usr/local/zed/pyzed-5.4-cp310-cp310-linux_aarch64.whl" }
